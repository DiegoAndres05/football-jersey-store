# TASK-008-003 Result

## Status: DONE

## Files Changed
- **Replaced** `tests/featured-carousel-slides.test.ts` — 10 TDD tests covering all business rules for `slidesForFeaturedCarousel`.

## Acceptance Criteria
- [x] 0 products with photo → `[]`
- [x] 1 product with photo → `[]`
- [x] 2+ with `primaryImage.url` → those products (order preserved)
- [x] Missing/`null` image excluded
- [x] More than 5 with photos → length 5
- [x] Does not read or format `minPrice`

## Validation
```bash
npx tsx --test tests/featured-carousel-slides.test.ts
```

### Results: 10 tests — 5 GREEN, 5 RED (expected)

**GREEN (5)** — pass because stub returns `[]`, which coincidentally matches:
1. 0 products → `[]`
2. 1 product with photo → `[]`
3. All without `primaryImage.url` → `[]`
4. 1 with photo + 1 without → `[]`
5. Does not read `minPrice`

**RED (5)** — fail until TASK-008-004 implements the real selector:
1. 2 products with `primaryImage.url` → expected 2, got 0
2. Products without `primaryImage.url` are excluded → expected 1, got 0
3. More than 5 with photos → max 5 → expected 5, got 0
4. Respects custom `max` parameter → expected 3, got 0
5. 3 with photos + 2 without → expected 3, got 0

## Failures
None unexpected. All 5 red tests are the intended TDD signal for TASK-008-004.

## Remaining Risks
- None. Tests are clean and will turn green once TASK-008-004 implements the filter+slice logic.
