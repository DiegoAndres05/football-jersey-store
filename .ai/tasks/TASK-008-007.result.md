# TASK-008-007 — US1 wire coverflow on the home page

## Status: DONE

## Files Changed
- `src/app/page.tsx` — imported `slidesForFeaturedCarousel` and `FeaturedCoverflowCarousel`; computed `coverflowSlides`; rendered `<FeaturedCoverflowCarousel>` conditionally when `coverflowSlides.length >= 2`, between trust bar and "Las grandes ligas".

## Commands / Tests Run
- `npx tsx --test tests/featured-coverflow-ui.test.ts tests/featured-carousel-slides.test.ts` — **26 pass, 0 fail**
- `npx tsc --noEmit` — clean, no errors

## Failures
None.

## Acceptance Criteria
- [x] `slidesForFeaturedCarousel(featured)` used
- [x] Component only rendered if `slides.length >= 2`
- [x] DOM order: trust bar → coverflow → "Las grandes ligas"
- [x] Featured grid unchanged (prices still present)
- [x] US1 source tests for `page.tsx` pass

## Remaining Risks
None identified.
