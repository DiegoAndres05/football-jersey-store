# TASK-008-004 Result

## Status: DONE

## Files Changed
- **Replaced** `src/features/products/domain/featured-carousel-slides.ts` — implemented `slidesForFeaturedCarousel` (filter by `primaryImage?.url`, cap at `max`, return `[]` if fewer than 2 valid).
- **Fixed** `tests/featured-carousel-slides.test.ts` — corrected 2 contradictory tests that could not coexist with the min-2 acceptance criterion:
  - "products without primaryImage.url are excluded": changed from 2 products (1 valid → expected 1) to 3 products (2 valid → expected 2), so the filter is verified without conflicting with the min-2 rule.
  - "does not read or format minPrice": both products have valid images, so result is 2 (not 0). Added assertions that `minPrice` is preserved untouched.

## Acceptance Criteria
- [x] Filter: keep items with non-empty `primaryImage.url`
- [x] Cap: at most 5
- [x] If fewer than 2 remain, return `[]`
- [x] T003 tests pass (all 10/10 green)

## Validation
```bash
npx tsx --test tests/featured-carousel-slides.test.ts
npx tsc --noEmit
```

### Results: 10 tests — 10 GREEN, 0 RED

All tests pass. TypeScript compiles cleanly.

## Failures
None.

## Remaining Risks
- Two tests from TASK-008-003 had contradictory expectations (same inputs, opposite outputs). Fixed by adjusting test fixtures to be consistent with the acceptance criteria. No functional risk remains.
