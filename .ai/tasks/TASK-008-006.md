# TASK-008-006 US1 implement featured coverflow client

## Role
Developer

## SpecKit
`specs/008-carrusel-camisetas-inicio/tasks.md` — T006 [US1]

## Depends on
TASK-008-005. Run: `/ai-task TASK-008-006`

## Objective
Implement `src/features/products/components/featured-coverflow-carousel.tsx` as a client coverflow of real `ProductCardData` slides.

## Context
Inspiration: 3D coverflow. Forbidden: restaurant dishes, `/components/ui/3-d-coverflow-carousel.tsx`, `defaultDishes`, Unsplash/CDN food, View Menu, prices.

## Files / Areas to Inspect
- `src/features/products/types/product-types.ts`
- `src/components/ui/button.tsx`
- `src/components/home/hero-product.tsx` (image pattern — do not turn hero into carousel)
- `specs/008-carrusel-camisetas-inicio/research.md`

## Constraints
- Follow existing architecture and conventions.
- Do not introduce unrelated changes.
- Do not install framer-motion.
- Do not use window-level arrow key listeners; scope keyboard to the focused `<section>`.
- Do not show `minPrice` / `formatMoney`.
- Lucide icons, not inline restaurant SVGs.
- `next/image` for catalog photos (and blurred backdrop if present).

## Acceptance Criteria
- [ ] Props: array of `ProductCardData` (slides already filtered by caller)
- [ ] Index, prev/next, dots, swipe
- [ ] Autoplay 5s; pause hover/focus; no autoplay when `prefers-reduced-motion`
- [ ] CTA “Ver camiseta” → `/productos/${slug}` via existing `Button` (`asChild` + `Link` OK)
- [ ] Title from `name` (team name optional subtitle)
- [ ] Side cards bring that slide to front (do not navigate yet)
- [ ] If `items.length < 2`, render `null`

## Validation
```bash
npx tsc --noEmit
npx tsx --test tests/featured-coverflow-ui.test.ts
```
Page.tsx placement asserts may still fail until TASK-008-007.

## Expected Report
Write `.ai/tasks/TASK-008-006.result.md` from `.ai/templates/result.md`.
- Status
- Files changed
- Commands/tests run
- Failures
- Remaining risks
