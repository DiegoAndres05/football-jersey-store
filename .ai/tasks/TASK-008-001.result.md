# TASK-008-001 Result

## Status: DONE

## Files Changed
- **Created** `src/features/products/domain/featured-carousel-slides.ts` — exports `slidesForFeaturedCarousel(products, max?)` returning `ProductCardData[]` (stub returns `[]`).

## Acceptance Criteria
- [x] File exists and exports `slidesForFeaturedCarousel`
- [x] Signature accepts `ProductCardData[]` (with optional `max` argument)
- [x] Return type is `ProductCardData[]`

## Validation
- `npx tsc --noEmit` — **PASS** (2 pre-existing errors from stale test `tests/featured-carousel-slides.test.ts` referencing old path `src/features/home/modules/featured-carousel/domain/slides.ts`; no errors from the new file).

## Failures
None related to this task.

## Remaining Risks
- The test file `tests/featured-carousel-slides.test.ts` references the old path and will need updating when T004 implements the real selection logic.
