# TASK-008-007 US1 wire coverflow on the home page

## Role
Developer

## SpecKit
`specs/008-carrusel-camisetas-inicio/tasks.md` — T007 [US1]

## Depends on
TASK-008-006. Same home file — do not parallel with unrelated page rewrites. Run: `/ai-task TASK-008-007`

## Objective
In `src/app/page.tsx`, render the coverflow after the trust bar and before “Las grandes ligas”, only when `slidesForFeaturedCarousel(featured)` has ≥ 2 items.

## Context
Keep hero (`HeroProduct` + `featured[0]`) and “Las más buscadas” (`featured.slice(0, 4)`). Do not paint the whole page `#0c0a09`.

## Files / Areas to Inspect
- `src/app/page.tsx`
- `src/features/products/domain/featured-carousel-slides.ts`
- `src/features/products/components/featured-coverflow-carousel.tsx`

## Constraints
- Follow existing architecture and conventions.
- Do not introduce unrelated changes.
- Do not remove or reorder hero, trust, leagues, featured grid, WhatsApp.
- Do not replace `HeroProduct` with the carousel.

## Acceptance Criteria
- [ ] `slidesForFeaturedCarousel(featured)` used
- [ ] Component only if slides.length ≥ 2
- [ ] DOM order: trust bar → coverflow → “Las grandes ligas”
- [ ] Featured grid unchanged (prices still there)
- [ ] US1 source tests for `page.tsx` pass

## Validation
```bash
npx tsx --test tests/featured-coverflow-ui.test.ts tests/featured-carousel-slides.test.ts
npx tsc --noEmit
```

## Expected Report
Write `.ai/tasks/TASK-008-007.result.md` from `.ai/templates/result.md`.
- Status
- Files changed
- Commands/tests run
- Failures
- Remaining risks
