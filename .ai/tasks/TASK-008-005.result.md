# TASK-008-005 Result

## Status
completed (expected-red)

## Summary
Wrote 16 source-contract TDD tests covering all US1 acceptance criteria. 13 fail as expected because `featured-coverflow-carousel.tsx` does not exist yet (T006/T007). 3 pass (page structure checks that already hold).

## Files changed
- `tests/featured-coverflow-ui.test.ts` — replaced placeholder with 16 source asserts

## Validation
- Command: `npx tsx --test tests/featured-coverflow-ui.test.ts`
- Result: 3 pass / 13 fail (expected-red)

## Failures
All 13 failures are caused by missing `src/features/products/components/featured-coverflow-carousel.tsx` (ENOENT) or the page not yet referencing `slidesForFeaturedCarousel`. This is the intended pre-implementation state — tests will turn green after T006 (component) and T007 (page wiring).

Failing tests:
1. coverflow component file exists
2. component uses slidesForFeaturedCarousel or exports FeaturedCoverflowCarousel
3. CTA text is Ver camiseta
4. CTA href contains /productos/
5. Button imported from @/components/ui/button
6. uses lucide ChevronLeft and ChevronRight
7. uses next/image
8. Spanish eyebrow: Destacadas or similar, not BEST SELLERS / View Menu
9. no restaurant demo artifacts in component
10. component does not render minPrice or formatMoney
11. autoplay interval ~5000ms
12. pauses on hover or focus
13. page imports or references slidesForFeaturedCarousel

Passing tests (already green):
1. page file exists and can be read
2. carousel placement: after trust bar and before Las grandes ligas
3. page does not contain restaurant demo artifacts

## Remaining risks
- Component file path assumed: `src/features/products/components/featured-coverflow-carousel.tsx`. If T006 uses a different path, adjust COMPONENT_PATH in the test.
- Placement test checks TRUST BAR comment and "Las grandes ligas" text order; if page markup changes, the order assertion may need updating.

## Notes for Cursor
Tests are TDD-red by design. T006 creates the component; T007 wires it into page.tsx. After both, re-run validation to confirm green.
